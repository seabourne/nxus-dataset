// ..js/datasets_form.js
import * as FieldUtil from '../../fieldUtils'


'use strict';

function checkForType(selectElement, fieldId, loadedValue) {
  console.log("type selected " + selectedElement.options[selectedElement.selectedIndex].value);
  
}